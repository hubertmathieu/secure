include mixins/base
html
    head
        meta(charset="utf-8")
        title="Demo | login"
        link(href="/stylesheets/style.css" rel="stylesheet")


    body

        .row
            .col-4
            .col-4
                #divElement
                    h1(id='deleteTitle') Êtes-vous certain de supprimer votre mot de passe
                    p(id='deleteText')="Commençant par " + password.password_content + " pour le site web " + password.web_site
                    a(href="/delete/" + password.password_id + "/confirmation")
                        button(type='submit' id='activeButton').btn.btn-outlin.button.mt-2 Supprimer
                    a(href="/manager")
                        button(type='submit' id='activeButton1').btn.btn-outlin.button.mt-2 Annuler
            .col-4